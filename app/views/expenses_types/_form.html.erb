<%= form_with(model: expense_type) do |form| %>
  <% if expense_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(expense_type.errors.count, "error") %> prohibited this expense_type from being saved:</h2>
      <ul>
        <% expense_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :expense_category, style: "display: block" %>
    <%= form.text_field :expense_category %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
